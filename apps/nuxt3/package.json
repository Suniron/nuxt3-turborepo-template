{
  "name": "@monorepo/nuxt3",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -p 3003",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf .nuxt && rm -rf .output",
    "lint": "eslint --ext .js,.ts,.vue --ignore-path .gitignore .",
    "lint:fix": "pnpm lint --fix",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxi typecheck"
  },
  "devDependencies": {
    "@monorepo/prettier": "workspace:*",
    "@monorepo/ui": "workspace:*",
    "@nuxt/devtools": "^0.6.7",
    "eslint": "^8.44.0",
    "eslint-config-custom": "workspace:*",
    "nuxt": "^3.6.2",
    "prettier": "3.0.0"
  }
}
